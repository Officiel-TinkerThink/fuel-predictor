# 02 — Which model made each prediction, where people look

**What to build:** the model code on the estimate (technical details), in the prediction history
(a *Model* column, searchable), in the bulk-prediction CSV, on model management and comparison
pages, in the API's model object and in the MCP `predict_fuel` result.

**Blocked by:** 01.

**Status:** in-review

- [x] The history lists each prediction's model code.
- [x] The API returns `model.model_code` with every prediction.
